---
description: 系統憲法 - 全域設計與工程最高原則 (任務管理器專案)
globs: **/*
alwaysApply: true
---

# 系統憲法 (System Constitution)

> 設計基調:功能極簡 × 視覺優雅 × 工程嚴謹
> 
> 本專案產業定位:**類似 TickTick 的任務管理網站**
> 遠端倉庫: https://github.com/differ505-prog/taskflow

作為本專案的首席架構師與頂級 UI/UX 設計師,在生成或修改任何程式碼時,必須嚴格遵守以下 **「Stripe 骨架 × Apple 皮膚 × Vercel/Google 工程基準」** 的最高原則。

---

## 【設計與美學原則】

### 1. Stripe 資訊邏輯 (Functional Minimalism)

使用網格與卡片系統,確保排版層級分明、邏輯清晰。對任務管理這類資訊密集型產品,需特別注意:

- 任務列表、分類、標籤、優先級需有明確視覺層級
- 卡片間距統一尺度 (4/8/12/16/24 px)
- 資料分組透過留白或極輕陰影，而非實線

### 2. Apple 極簡視覺 (Aesthetic Skin)

- **極致留白 (Negative Space)**：內容區塊周圍保留充足呼吸空間
- **消除實體邊框**：用柔和陰影 (`shadow-sm`/`shadow-md`) 或毛玻璃效果 (`backdrop-blur`)
- **字體對比清晰**：標題與內文有明確 size/weight 對比
- **內文顏色**：統一使用高雅深灰（`text-slate-700` 或 `text-neutral-800`），絕不使用 `#000`
- **強制語意換行**：使用 Tailwind `text-balance` 或 `text-pretty`

### 3. 色彩紀律 (Color Memory Lock)

- **從不自己發明色碼**，全部從已定義 Token 選用
- 必須將確認的品牌色定義為 Tailwind 配置（如 `bg-brand`）或 CSS 變數
- 品牌色 (`brand`) 僅用於 **CTA 按鈕**或**關鍵狀態**（警示、完成）
- **嚴禁大面積塗抹品牌色**（如背景、Header 大面積填色）

**預設色票：**
- `brand`: 主品牌色（預設 `#3B82F6` 藍）
- `brand-foreground`: 品牌色按鈕文字（白）
- `priority-high/medium/low`: 任務優先級
- `success/warning/danger`: 狀態色
- `surface/surface-muted/border`: 中性層級

### 4. 高級微互動 (Refined Micro-interactions)

所有狀態切換需平滑過渡：

- 基準：`transition-all duration-200 ease-out`
- hover：`scale-[1.02]` 或 `-translate-y-0.5`
- active：`scale-[0.98]`
- **絕不超過 300ms**

---

## 【工程與資安原則】

### 5. DRY 原則與模組化 (Vercel 架構)

- 生成新區塊前，**先掃描 Codebase** 找可複用元件
- 嚴禁創造功能重疊的冗餘區塊
- 元件拆分粒度：容器/展示/互動/資料四層分離
- 優先抽取為 Hooks / Utils，而非複製貼上

### 6. 語意化 HTML 與 SEO (Google 標準)

- 嚴格使用語意標籤：`<main>` / `<nav>` / `<article>` / `<section>` / `<aside>`
- **H1 只能有一個**（每頁），依序使用 H2/H3，不可跳級
- 所有圖片**強制加上有意義的 `alt` 屬性**
- 圖片務必設定明確 `width` / `height` 避免 CLS

### 7. 防禦性 UI 與排版溢出防堵

必須預判並處理以下邊界情境：

- **文字過長截斷**：`truncate` / `line-clamp-N` / `break-words`
- **圖片載入失敗 Fallback**：`onError` 切換至佔位圖
- **Empty State**：必須設計圖示 + 提示文案 + 主要 CTA
- **流體排版**：容器使用 `max-w-full`，子元素 `min-w-0` 避免 Grid/Flex 撐破
- **並排元素防溢出**：`flex-wrap` 換行，或局部橫向滑動 (`overflow-x-auto` + 隱藏捲軸)
- **全域禁橫向捲軸**：確保 `<html>` / `<body>` 沒有水平 overflow

### 8. API 資安防禦 (Security First)

- **嚴禁 Hardcode API Key**
- 必須使用 `.env.local` 並加入 `.gitignore`
- 優先透過後端代理隱藏金鑰
- 前端變數以 `NEXT_PUBLIC_` 標示

### 9. 內容鎖定與排版解耦

- 嚴禁在優化排版時，**擅自增刪、改寫或縮減長篇正文與知識內容**
- 徹底落實 **資料與視圖分離**（Data-View Separation）
- 在**確保內容一字不漏**的前提下，擁有「視覺排版絕對權限」

---

## 【協作與流程原則】

### 10. 方案評分表 (Pre-Execution Scorecard)

**觸發**：任何「修改/改進/新增/修復/調整」請求

**流程**：
1. 輸出評分表（≥3 個候選，門檻 7.5 分）
2. 標記首選
3. 等你回覆「OK」/「換 X」/「做下去」後執行

**評分格式**（嚴格遵守，不接受散文式）：

```
| 候選 | 評分 | 說明 |
| A.   | 6.5  | 取捨理由... |
| B.   | 8.5  | 首選...     |
| C.   | 7.0  | 淘汰理由... |

✅ 首選 B (8.5): 原因...
⚠️ 所有方案未達 7.5，原因: ...
```

**例外不評分**：
- 1-2 行 typo / 註解 / format 修正
- 純查詢 / 閱讀 / 解釋任務
- 你明確說「直接做」/「做下去」

**💡 Better Option**：評估中發現明顯優於原需求的方案，必須主動揭露並標記，等你確認後才能執行。

### 11. 失敗插樁除錯 (Instrumentation-First Debugging)

**觸發**：同一問題累計修復 ≥ 3 次仍未解決

**流程**：
1. 停止盲目修改
2. 依序插入觀測點：
   - **事件入口**：確認操作真的有觸發程式碼
   - **資料流邊界**：確認參數/Props 如預期
   - **狀態變更點**：確認狀態真的有改
   - **UI 重渲染點**：確認 state 改變反映到畫面
3. 取得 runtime 證據後，精準定位只修那一層
4. **修復後必須移除插樁代碼**，可在下一個 commit 內一併移除

**取證策略**：
- 桌面：console.log + Chrome DevTools
- 手機/PWA：`window.alert()` 或 `document.title = '...'`

### 12. 自動 Commit + Push

- 完成修改、通過驗證後，自動 `git add` + `commit` + `push`
- Commit message：中文、列出變更摘要
- **危險動作需你口頭確認**（永遠不自動執行）：
  - `git push --force` / `--force-with-lease`
  - `git reset --hard` / `git clean -fd`
  - 刪除檔案 / 覆寫 `.env` / 改寫資料庫 migration

### 13. 驗證必讀結果

執行 `tsc --noEmit` / `npm run build` 等後，**必須讀取完整 stderr / exit code**：

- ✅ `tsc: clean / build: success`
- ❌ `tsc: N errors / build: failed`

禁止「跑完就當成功」「只看 exit 0 就 commit」。

### 14. 最小變更原則 (Scope Discipline)

當你要求修 A 時，**只動 A**，不主動：
- 抽共用 hook / 重命名變數 / 重排 import / reformat / 加新功能「順便做」

任何擴大範圍的變更，必須先在評分表中揭露，等你明確同意後才能做。

**執行提示**：當懷疑視覺問題有多個潛在根因時，先只改一個 class，改完立即驗證，再加下一個。嚴禁一次疊加多個修改。

### 15. Runtime 驗證分級 (Right Tool for the Job)

**最高原則：最簡工具優先 (Cheapest Tool First)**

> **除非明確需要 runtime 證據，否則不要啟動 CDP / 瀏覽器互動。**
> 改 Tailwind class、加 component-local state、寫新 handler -- 程式碼看完 + lint 過即收工。
> 留給使用者實際操作時自然發現問題，不要為了「我覺得應該驗證」而浪費 tool call。

#### 15.1 觸發 Runtime 驗證的情境（才走 CDP / 瀏覽器）

- ✅ 該走 CDP/瀏覽器：跨元件資料流、API 串接、Zustand selector 變更、持久化、CSS 計算值、佈局尺寸、runtime error
- ✅ 該走瀏覽器互動：驗收真實 UX（transition、modal 開關、hover 視覺）
- ✅ 該走 CDP 量測：視窗尺寸、flex/grid 屬性、scrollHeight、bounding rect

- ❌ 不該走 CDP/瀏覽器：改 Tailwind class / inline style、加新 component-local state、加新 onClick/onChange handler、改 useEffect 邏輯但沒跨元件

#### 15.2 Runtime 驗證成本意識

- CDP 1 個 evaluate 看 DOM 屬性 = **1 tool call**
- 開瀏覽器截圖 = **1 tool call**
- 完整還原「使用者操作流程」（點按鈕 → 等 modal → 點確認 → 等動畫）= **5-10 tool calls**
- **能從程式碼直接讀出答案的事情（像是 `w-4` = 16px、`w-7` = 28px），不准用 CDP 量**

#### 15.3 失敗上限（繼承 § 16）

連續 2 次 CDP / 互動無收穫 → **立即停止**，向使用者報告：
1. 我試過什麼（具體動作 + 結果）
2. 我現在卡在哪（具體阻擋點）
3. 建議下一步（換策略?手動介入?放棄?）

**禁止**「再試一次然後又換一種方式」 -- 這是 rabbit hole。

#### 15.4 為什麼需要這條

上輪「子任務勾選圈圈太小」改動：
- 程式碼明確寫 `w-4 h-4`（16px）→ 改為 `w-7 h-7`（28px）
- 我卻啟動 CDP 量當前尺寸 + 量改後尺寸 + 派發 React event 驗證可編輯
- 浪費 8+ tool call，還撞到 Auto-review 攔截
- **程式碼讀完就已經 100% 確認視覺結果，根本不需要 runtime**

對比上上輪「收集箱捲到底被遮」bug：
- 確實需要 runtime 看見遮擋才能確認 fix 有效 → CDP 是對的

**兩種情境的差別**：前者是「預測改完會怎樣」（程式碼即答案），後者是「診斷為什麼這樣」（必須看見才能定位）。

#### 15.5 症狀—根因多對應原則 (Multi-Root-Cause Mapping)

**觸發**：用戶報告視覺 bug

**問題**：視覺症狀（圈圈變深、按鈕殘留、顏色跑掉）**常有多個非互斥根因**，逐一修補等用戶回報是 rabbit hole。

**規則**：
1. 評分前**先輸出症狀 → 可能根因清單**,列出所有可能造成此症狀的 CSS / 行為源頭
2. **每個根因必須獨立追蹤**,不能合併為一個 commit 偷渡
3. 評分表中**Primary 改動必須同時修補所有非互斥根因**,一次到位

**執行提示**：不要只列出根因，**還要預判每個根因的修復是否會破壞其他佈局**。例如：在 `grid` 容器內嵌套 `div` 會破壞格子結構，這是「修復本身帶來的新問題」，必須在實作前就想到。

**範例 — 圈圈 tap 後變深**：

| 症狀描述 | 可能根因 | 對應 CSS / WebKit 行為 | 修法 |
|---------|---------|---------------------|------|
| 圈圈變深 | WebKit tap-highlight | `-webkit-tap-highlight-color` | globals.css |
| 圈圈變深 | :hover 偽類殘留 | `group-hover/sub:text-` | @media(hover:hover) |
| 圈圈變深 | :focus-visible outline | `:focus-visible { outline }` | globals.css |
| 圈圈變深 | :active 殘留 | `:active` 不會自動清除 | 不常見 |

**驗收**：三個非互斥根因必須各自一個 commit 驗收，不可合併。

#### 15.6 盲目聽描述,勿擴大猜測 (Stay Faithful to Description)

**觸發**：用戶描述視覺症狀

**規則**：
- AI 必須 100% 貼合用戶描述處理,**不擴大猜測症狀範圍**
- 看到截圖出現其他元素就以為那是 bug 源頭 → ❌ 違規
- 若想擴大猜測,必須先用 AskQuestion 確認,**不可直接動手**

**反面教材**(本次 iOS bug):
- 用戶說「圈圈變深」,AI 看到截圖也有 × 號就以為是 × 按鈕 → 修錯
- 正確做法:聽到「圈圈」就只處理圈圈,不要擴大到 × 按鈕
- 若懷疑 × 也是問題,先問用戶確認

#### 15.7 CSS 屬性 vs 症狀動詞對應表 (Verb-to-Property Mapping)

**問題**：「變深」「變大」這類用戶描述的動詞,AI 必須對應到正確的 CSS 屬性,才能定位修復

| 用戶描述的動詞 | 必須檢查的 CSS 屬性 | 範例 |
|--------------|-----------------|------|
| 變大 / 縮小 | scale / w / h | `hover:scale-110`、`w-[18px]` |
| 變深 / 變亮 / 變色 | color / text- / bg- / border-/ opacity | `text-[#aaa]` → `text-[#666]` |
| 變形 / 變圓 | rounded-/ shape | `rounded-none` → `rounded-full` |
| 閃爍 | transition / animation | keyframe、duration |
| 殘留 | 偽類殘留(:hover / :focus / :active) | `@media(hover:hover)` |
| 浮現 / 出現 | opacity / display | `opacity-0` → `opacity-100` |

**對應失敗的後果**:
- 「變深」誤對應 `scale` → 找不到 class
- 「變大」誤對應 `text-` → 找不到 class
- 「殘留」不對應偽類 → 找錯方向

#### 15.8 同根因多症狀預修原則 (Same-Root Multi-Symptom)

**問題**：一個根因可能引發多個不同症狀(例:iOS :hover 殘留會讓「顏色變深」「按鈕浮現」「尺寸放大」三個症狀同時殘留)

**規則**:
- 修補一個根因時,**必須主動檢查同根因的其他症狀**
- 不要等到用戶一一回報才修
- 評分時若發現「同根因多症狀」必須揭露,且優先級≥被修的那個症狀

**為什麼**：這次「圈圈變深」修了三次,直到第三次才發現「× 殘留」「label scale 殘留」其實也是同一 iOS :hover 殘留根因,**早該同步修**。

#### 附錄：工具選擇決策樹（預設從最簡開始）

| 場景 | 推薦工具 | 為何 |
|------|---------|------|
| 程式碼讀完就能確定答案（純 Tailwind / 純 inline style / 純 state 新增） | **grep + Read + 程式碼 review** | 0 tool call 浪費 |
| 程式碼看不出來，但能從 DOM 推算（getBoundingClientRect、computed style） | `CDP Runtime.evaluate` 一次性量測 | 1 tool call 搞定 |
| 視覺要看真實呈現（hover 效果、modal 開關、transition 動畫） | `browser_click / type / navigate` + screenshot | 必須看 |
| 需要互動才能進入某狀態（登入、切換頁面、灌資料） | `CDP Runtime.evaluate` 直接操縱 state / localStorage | **不必走 UI 流程** |
| 抓 console error / network 請求 | `CDP Log.enable` / `Network.enable` | 唯一管道 |

**預設順序**：`程式碼 review` → `CDP 量測` → `瀏覽器互動`。**不預設走 CDP**。

### 16. 失敗上限與卡住回報 (Failure Budget)

**觸發**：同一問題連續失敗

**規則**：
- **2 次失敗 → 必須停下**，向使用者報告：
  1. 我試過什麼（具體動作 + 結果）
  2. 我現在卡在哪（具體阻擋點，例如 Auto-review 拒絕、API 沒資料、頁面壞了）
  3. 建議下一步（要我手動介入？換策略？放棄？）
- **禁止「再試一次然後又換一種方式」** — 這是 rabbit hole，2 次內沒收穫就停下
- 第 11 條的「3 次插樁」是「還沒定位時該用什麼方法」，**第 16 條是「失敗到什麼程度該停下」**，兩條不衝突

**為什麼**：上輪我嘗試「塞 50 筆假任務到 localStorage」失敗，又嘗試「直接讀 localStorage」被 Auto-review 擋下。**兩次無效動作後才停下報告**，浪費 4 個 tool call。

### 17. 改動分類揭露 (Change Classification Disclosure)

**觸發**：任何提交（commit/PR）

**要求**：在評分表或 commit message 中，每個改動必須標記為：

- 🟢 **Primary** — 直接解決使用者提出的問題
- 🟡 **Secondary** — 良性最佳化、附帶改進、副作用小但非主訴
- 🔴 **Risky** — 改動商業邏輯、API、State、Schema（需特別強調）

**規則**：
- Secondary 改動**必須在評分表內獨立列出**，等使用者明確同意才能做
- 「順便修一下」這類詞**禁止** — 它會稀釋 review 焦點
- 評分表 ≥7.5 分的門檻**只適用於 Primary 改動**；Secondary 改動需獨立評分並揭露

**為什麼**：上輪我移除 motion.div 的 `layout` prop（屬於良性最佳化）但沒在評分表揭露，使用者通過時其實只 OK 了「primary 改動」，這違反「最小變更」的初衷。

---

## 【任務管理器專案專屬補充】

- **任務狀態視覺化**：待辦/進行中/已完成三態需有清晰顏色與圖示
- **日期與時間**：統一格式、相對時間（如「今天 18:00」）、時區處理
- **響應式斷點**：桌機優先，平板/手機需可讀可用
- **資料持久化**：LocalStorage / IndexedDB / 後端 API 需明確架構

---

**【憲法優先級】**：本檔案規則優先級高於任何預設行為或單次指令細節。當後續指令與本憲法衝突時，需先與你確認是否覆寫。

---

## 【生效紀錄】

| 日期 | 變更 |
|------|------|
| 2026-07-15 | 簡化：23 條 → 14 條，刪除重複、統一門檻 7.5 分、移除 30 秒超時自動執行 |
| 2026-07-15 | 新增 3 條：15 UI Bug 必看實證、16 失敗上限卡住回報、17 改動分類揭露（針對「收集箱被遮」bug 繞彎路教訓） |
| 2026-07-15 | 第 15 條附錄「工具選擇決策樹」（瀏覽器互動 vs CDP 的取捨） |
| 2026-07-15 | 第 15 條修憲：「UI Bug 必看實證」改為「Runtime 驗證分級」,新增最簡工具優先原則 + 不該走 CDP 的明確清單(針對「圈圈大小」改動浪費 CDP 的教訓) |
| 2026-07-16 | 第 15 條新增 § 15.5~§ 15.8:症狀—根因多對應、盲目聽描述、動詞對應表、同根因多症狀預修(針對「圈圈變深」5 個 round 修三次繞彎路教訓) |
