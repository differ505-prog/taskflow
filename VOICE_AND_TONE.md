# Voice & Tone Guide（品牌口吻指南）

> SSOT：所有 UI 文案的唯一參考。
> 目標：「Less is more」落實到字句選擇 — 精簡、溫柔、有靈魂。

---

## 1. 品牌口吻（Brand Voice）

### 核心三詞

| 詞 | 意義 | 對應 OK | 對應 NG |
|---|---|---|---|
| **溫柔** | 不責備、不催促、ADHD 友善 | 「記得再回來看它」 | 「您有 X 項過期任務待處理」 |
| **精準** | 動詞為主、名詞為輔；字字有承擔 | 「刪除這個清單」 | 「您確定要執行刪除操作嗎？」 |
| **儀式感** | 把平凡動作賦予意義 | 「無罪赦免」「復活」「醞釀中」 | 「歸檔」「取消」「開發中」 |

### 不變的底線

- ✅ 始終用「你」不用「您」 — 平等、拉近距離
- ✅ 動詞優先於名詞；按鈕能用 2 字就不用 4 字
- ✅ 錯誤訊息給「下一步」而不是「發生了什麼」
- ❌ 沒有「建議」「您可以」「請嘗試」這種客服腔
- ❌ 沒有「過期」「逾期」「失敗」這種責備詞
- ❌ 沒有 emoji 標點化（句尾加 🎉 之類）—— emoji 是強調，不是標點

---

## 2. 七個情境 × 推薦文案模板

| 情境 | 文案定位 | 範例 |
|---|---|---|
| **成功 Toast** | 完成動詞 + 結果，1 行 | 「已排定為今日」「已加入收集箱」 |
| **錯誤 Toast** | 一句話講清楚 + 行動建議（可選） | 「Email 或密碼錯誤」「推播被拒絕，到 iOS 設定 → Safari 開啟」 |
| **空狀態** | 視圖定位 + 動作動詞 | 「腦中先倒乾淨」「倒一筆進來」 |
| **確認對話框** | 主按鈕是「動作動詞」**絕不**是「確認」 | 「永久刪除」「我決定放棄」「無罪赦免」 |
| **404** | 帶品牌記憶點（呼應失物招領系列） | 「這頁面飄走了 — 失物招領處請見左側」 |
| **Not yet（幽靈功能）** | 誠實幽默 + 期待感 | 「⏳ 時間感知魔法醞釀中...」 |
| **Loading** | 動作進行式，不用「Loading」 | 「寄送中...」「建立中...」「套用中...」 |

---

## 3. Emoji 紀律

| 規則 | 說明 |
|---|---|
| **情境化使用** | 只在強調「儀式/驚喜/復原」時使用 (✨ 🍃 ✓ ⏳) |
| **禁用標點化** | 句尾「...🎉」「...✨」→ 移除 emoji |
| **同一句最多 1 個** | 多 emoji = 訊號稀釋 |
| **勿用於錯誤** | 錯誤訊息永遠不用 emoji（製造的應該是信任，不是快樂） |

---

## 4. 確認對話框按鈕動詞庫

主按鈕必須是「動作動詞」，禁用「確認」「確定」這種抽象詞。

| 動作類型 | 推薦動詞 |
|---|---|
| 不可逆刪除 | 「永久刪除」「徹底移除」「無罪赦免」 |
| 一般刪除 | 「刪除」「拿掉」「放下」 |
| 封存 | 「送進封存」「暫時放下」 |
| 覆寫 | 「覆蓋」「替換」「改為」 |
| 登出 | 「登出」「安全離開」 |
| 中斷 | 「結束」「離開這裡」「放棄」 |
| 預設 | 「好，下一步」「繼續」 |

副按鈕永遠是「取消」「返回」「先不要」 — 不要「返回選擇」這種工程腔。

---

## 5. 錯誤訊息翻譯層原則

所有外部錯誤（Supabase、Push API、Network）**必須**經過 `src/lib/errorMessages.ts` 翻譯。
- ❌ 直接 `toast.error(e.message)` — 曝露工程詞
- ✅ `toast.error(translateError(e, "context"))` — 統一轉譯

格式：「發生了什麼 + 為什麼 + 下一步」三段。但**全段 ≤ 35 字**。

---

## 6. 範例對齊表

| 場景 | Before（現行） | After（標準） |
|---|---|---|
| 新增任務按鈕 | 「新增任務」 | 「建立一個任務」或「倒一筆進來」（語境） |
| 404 文案 | 「頁面不存在」 | 「這頁面飄走了」 |
| 確認刪除 | 「確認」 | 「永久刪除」 |
| 推播被拒絕 | 「通知已被拒絕，請到 iOS 設定 → Safari → 網站資料 → 清除後重試」 | 「推播被拒絕，到 iOS 設定 → Safari 開啟」 |
| Toast 同步完成 | 「已同步」 | 「已同步 ✓」（emoji 是訊號，不是標點） |
| Loading 文字 | 「Loading...」 | 「載入中...」 |

---

## 7. 例外條款

- **LostAndFound**：「無罪赦免」「復活」是品牌資產，不可改為「封存」「延後」
- **FeedbackButton / ProWaitlistModal**：emoji + 儀式語態，這是品牌的「驚喜時刻」
- **AuthPage 錯誤**：直白、技術性可接受（因為使用者此刻正在 debug 帳號問題）

---

維護者：所有新元件 / 新文案 / 新錯誤訊息，**對齊這份 SSOT**。
當 SSOT 與新需求衝突 → 修 SSOT（在本檔案新增條目），不要偷偷違反。
