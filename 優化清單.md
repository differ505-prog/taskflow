# 任務管理器 Wade - 產品規格書

> 靈感來源：Apple Reminders
> 設計哲學：Stripe 骨架 × Apple 皮膚 × Vercel 工程基準

---

## 已實作優化

| 功能 | 狀態 | 日期 |
|------|:----:|------|
| 單擊卡片即現詳情面板 | ✅ | 2026-07-15 |
| 子任務即時展開可見 | ✅ | 2026-07-15 |
| 廢除展開/折疊 UI | ✅ | 2026-07-15 |
| 手機版清單篩選入口 | ✅ | 2026-07-17 |
| 收集箱任務清單尾部任務被遮蔽 | ✅ | 2026-07-17 |
| OmniSonic iframe 整合（Pomodoro Timer 內嵌入音樂按鈕） | ✅ | 2026-07-19 |
| isUrgent TypeError 修復（§26 類別 A：priority undefined/LegacyPriority 落 Q4） | ✅ | 2026-07-19 |
| 詳情面板輸入即儲存（TickTick 直覺：13 欄位 debounce 300ms + 完成按鈕 = handleSaveAndClose） | ✅ | 2026-07-19 |
| 持續輸入保護（§26 類別 A 子模式：editingTaskIdsRef + 30 秒 keystroke 延長窗） | ✅ | 2026-07-19 |

---

## OmniSonic 整合紀錄（跨 repo：differ505-prog/music-focus-environment）

> 整合時間：2026-07-19
> 整合目的：Pomodoro Timer focus 模式下嵌入 OmniSonic Deep Focus 按鈕，點擊後跨 iframe postMessage 觸發 ZenFlowPanel 自動 DJ 播放。
> 環境變數：`NEXT_PUBLIC_OMNISONIC_URL=https://music-focus-environment.vercel.app`（Wade Vercel production 已設定）
> 通訊協議：embed page 發送 `OMNI_START_RANDOM` / `OMNI_PLAYING` / `OMNI_ERROR` postMessage，Wade 端 `ZenFlowContext` 監聽

### Phase A：OmniSonic embed page 移除 GlobalPlayer UI（避免入侵 Wade iframe）

- **commit hash**：`adf8c52`（位於 music-focus-environment repo）
- **問題**：embed page `src/app/layout.tsx` 標 `'use client'` 與 `export const metadata` 衝突 → Vercel build 失敗 → 上一版 `f6a7f72` 的 embedMode 改動實際未部署、SSR HTML 仍含 GlobalPlayer
- **根因**（命中 §26 已知類別 + §25 既有 pattern 對齊）：server-only API（metadata）與 client-side API（usePathname）混用
- **修法**：拆 layout 為 server component（保留 metadata export）+ 新增 `src/components/embed-route-guard.tsx`（client component 內含 usePathname 判斷）
- **驗證**：curl `https://music-focus-environment.vercel.app/embed/button` → SSR HTML 只含「開始深度專注心流」按鈕、GlobalPlayer 完全消失 ✅
- **§26 命中**：客戶端/伺服器元件邊界 + metadata server-only

### Phase B：Wade iframe 整合手動驗收

- **驗收方式**：你親自登入 `taskflow-v2.vercel.app` → 進 Pomodoro Timer（focus 模式）→ 看到 OmniSonic 嵌入按鈕 → 點擊測試 → 確認播放/暫停/切歌 + ZenFlowPanel 展開
- **驗收結果**：✅ 整合正常、跨 iframe postMessage 通訊成功、音訊播放成功
- **未自動化驗證原因**：登入需 Firebase auth credentials，屬 §15.1 blocker；MCP `browser_lock` 被 Auto-review 阻擋；runtime tool call 預算已用 3/6

### 跨 repo commit 對應表

| 改動 | repo | commit | Vercel 部署 |
|---|---|---|---|
| embed 移除 GlobalPlayer | music-focus-environment | `adf8c52` | ✅ 已 deploy |
| iframe 嵌入（Pomodoro Timer） | taskflow | 既有 feature | ✅ production |

---

## 待實作功能

> **排序原則**：每次實作前重新檢視現狀，優先修復「目前狀態最影響使用」的項目，而非僅按原始優先級。
> - 排序維度：`使用影響程度`（完全不可用 > 功能異常 > 體驗不順）
> - 每個項目需追蹤「當前狀態」：完全未修 / 部分修復 / 堪用但有瑕疵

| 優先級 | 功能 | 當前狀態 | 備注 | 狀態 |
|:------:|------|-------------------|:----:|
| **P1** | 今天視圖 Quick Add 按 Enter 觸發 macOS Finder 選擇檔案 | 部分修復 | 已封鎖 Enter，需重新評估最佳 UX 方案 | ❌ 未修好 |
| **P1** | 今天視圖新增任務未出現在今天區塊 | 修復中 | 時區全面改本地（commit 9e871e4）；race condition 修復（commit ea81071）| 🔎 待測試 |
| **P1** | 刪除任務 lag 才消失（Desktop Chrome）| 已修復 | 同步層初次訂閱空資料覆蓋本地 state，commit bedfa93 | ✅ 已修好 |
| **P1** | 任務詳情面板：點擊任務後面板應出現在右側，但目前顯示不完整並出現在左側 | 已修復 | 面板移至 flex 同層 sibling，commit b73e8c6 | ✅ 已修好 |
| **P2** | 新增子任務 bar 放在子任務區塊上方 | 已修復 | 移到標題列下方、列表上方（TaskDetailPanel.tsx），順序：標題→輸入欄→子任務列表 | ✅ 已修好 |
| **P2** | 任務置頂功能 | 未實作 | 需確認：每個 list 獨立置頂 vs 全域置頂 | 🔎 待確認 |
| **P2** | 主搜尋補上子任務標題匹配 | 已修復 | AppContext 補上 subTasks.title.includes(q) | ✅ 已修好 |
| **P2** | 蕃茄鐘綁定任務改為可搜尋 combobox | 已修復 | 取代原生 <select>，輸入即時過濾；主搜尋框 140→220px、placeholder 改為「搜尋任務 / 子任務 / 標籤」 | ✅ 已修好 |
| **P2** | 日曆視圖已完成任務自動折疊 | 已修復 | CalendarView 任務列表分兩段渲染（未完成永遠展開、已完成預設折疊） | ✅ 已修好 |
| **P2** | 手機版蕃茄計時入口 | 已修復 | BottomNavigation「更多」popover 頂部新增蕃茄計時入口（brand 色 Timer icon） | ✅ 已修好 |
| **P2** | 任務詳情面板已完成子任務可摺疊 | 已修復 | 沿用 useSubTaskCollapse hook，與 TaskListItem 行為一致；用戶 uid 為 scope | ✅ 已修好 |
| **P1** | 中文輸入法選字 Enter 誤觸建立任務/子任務 | 已修復 | 全 codebase 8 處 Enter 提交路徑補上 `isComposing` 防護（src/utils/imeGuard.ts 共用 helper）：AppShell quick add x2、TaskForm form submit + 標題 + 標籤選字 + 子任務、TaskDetailPanel 子任務 + 標籤、TaskQuickActions、TaskCommentsInline/Drawer、TagsPage 建立/重新命名 x4、SwipeableSubTask 編輯 blur、SettingsPage Beta Email | ✅ 已修好 |

---

## Supabase Console 錯誤修復（2026-07-19 觸發）

> **觸發**：用戶要求「輸入即儲存」修法評分表時,順帶揭露 production console 3 個 supabase 報錯
> **commit hash**：`89524de`（已 commit 未 push，等用戶跑 SQL 驗證有效後再 push）
> **檔案位置**：`supabase/migrations/0013_realtime_and_profile_fixes.sql`
> **環境要求**：在 Supabase Dashboard SQL Editor 手動執行（§11 危險動作）

### 3 個錯誤及其根因

| 錯誤 | 根因 | 修法 |
|---|---|---|
| `upsert_profile` 400 (Bad Request) | 0006 migration 未跑,舊版 `upsert_profile` 函式簽章不匹配 | 重新 `create or replace` 最新版（自動 role 計算） |
| `get_user_role` 404 (Not Found) | 0006 migration 未跑,函式不存在 | 重新 `create or replace`（idempotent,若 0006 已跑則 or replace 覆寫） |
| `postgres_changes not allowed` | 6 個表（personal_tasks / personal_lists / shared_lists / shared_tasks / shared_list_members / beta_users）從未加入 `supabase_realtime` publication | `alter publication supabase_realtime add table` × 6（IF NOT EXISTS 守護，**idempotent**） |

### 用戶待辦（重要，**這是 §11 危險動作清單**）

1. 打開 Supabase Dashboard → SQL Editor
2. 複製 `0013_realtime_and_profile_fixes.sql` 全部內容（163 行）
3. 貼上執行
4. 回到瀏覽器刷新 `taskflow-v2.vercel.app`
5. console 應該乾淨；同時 **realtime 即時同步會啟用**（全新的功能：另一個裝置改了任務會自動同步到你的畫面）
6. 驗證完成後告訴 Cursor，「可以 push `89524de`」

### 預期效果

- ✅ Console 3 個錯誤消失
- ✅ 列表即時同步（自己的 A 分頁改了,B 分頁即時看到；不同裝置登入同帳號即時同步）
- ✅ 子任務即時同步
- ⚠️ 第一次啟用 realtime 後,supabase realtime quota 會被這個 app 持續使用（按 supabase 計價模式）

---

## 已廢除功能

| 廢除項目 | 原因 | 廢除日期 |
|----------|------|----------|
| TaskCard 展開/折疊 | 點擊卡片直接開編輯面板，展開已無意義 | 2026-07-15 |
| ChevronDown 箭頭 | 已無展開狀態，移除誤導 UI | 2026-07-15 |
| 「收起」按鈕 | 已無折疊功能 | 2026-07-15 |
| AnimatePresence/motion | 移除展開動畫，減少依賴 | 2026-07-15 |

---

## 核心 UX 原則

1. **Immediate Revelation** — 資訊優先可見，減少點擊
   - 子任務預設展開，不需要展開/折疊操作
   - 點擊任務卡片 → 開啟編輯面板

2. **Smart Defaults** — 智慧分類減少手動操作
   - Today / Upcoming / Flagged 等自動過濾視圖

3. **Visual Hierarchy** — 清晰的視覺層級
   - 已完成任務：灰階 + 刪除線
   - 任務間距充足，呼吸感強

---

## 技術棧

- **Framework**: Next.js 15 (App Router)
- **Styling**: Tailwind CSS
- **State**: React useState + LocalStorage
- **Font**: Inter (Google Fonts)
