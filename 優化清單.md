# TaskFlow 品牌系統升級

> **狀態：✅ 完成（commit 待 push）**
> 時間：2026-08-11

---

## ✅ 已經做完

| # | 任務 | 驗證 |
|---|---|---|
| 1 | 新增 `--accent-warm` CSS token（light + dark mode 各一組） | ✅ build clean |
| 2 | 焦點卡呼吸光暈改用暖紫色（light mode） | ✅ |
| 3 | 焦點卡 hover 光暈改用暖紫色 | ✅ |
| 4 | 完成波紋動畫改用暖紫色 | ✅ |
| 5 | ZenDashboard EmptyState 加暖色光暈 + 升級按鈕語義 | ✅ |
| 6 | FlowTimer 已有紫色基因（保持現狀） | ✅ |

**新增 Token：**
```css
--accent-warm-start: #A78BFA;  /* 紫羅蘭 */
--accent-warm-end: #818CF8;     /* 靛藍 */
--accent-warm-tint: rgba(167, 139, 250, 0.12);
--accent-warm-glow: rgba(129, 140, 248, 0.25);
```

---

## 🪞 Architect Mirror

- [PASS] 無新失序揭露

---

---

# TaskFlow 推播系統 — 醒來接手指南

> **狀態：90% 完成，等你醒來 5 分鐘收尾**
> 部署版本：dpl_AtzxxfDeMJdWx3ksTFDkMBWSLS1R (v4)
> 主網域：https://www.vibelist.work
> 時間：2026-08-01 22:16 (UTC+8)

---

## ✅ 已經做完（自動跑的）

| # | 任務 | 驗證 |
|---|---|---|
| 1 | Supabase migration 建立 `push_subscriptions` / `notification_log` / `cron_runs` | ✅ |
| 2 | Vercel env 設定 8 個加密變數 | ✅ |
| 3 | Cron 改 `0 9 * * *` (Hobby plan 限制) | ✅ |
| 4 | Production redeploy 4 次 | ✅ |
| 5 | VAPID 金鑰旋轉（新公鑰已寫入前端 bundle + 後端 env） | ✅ |
| 6 | Middleware 修正（`/api/push/*` `/api/cron/*` 從 protected 改 public） | ✅ |
| 7 | Dark Mode 光暈一致性：keyframes 從 hardcode hex 改 rgba(var(--accent-warm-*-rgb)) | ✅ |curl 觸發 `/api/push/send` → HTTP 200 + sent=0（無訂閱是預期） | ✅ |

**新公鑰（記下來，確認比對）**：
- Public: `BMkZA2s-HcC9554layYDt7aIjzXyMghnNEbiaqTTDIB5ALszTuUHsRdlj5KhUXaBKaOwpgAxhL13Jegs0Kk1zf0`
- Private: `N8ZeTYMvbIM5V_8yZh5dwRhoN-7cTGqGI_7oNUYVfn0`

---

## ⏳ 醒來要做的事（5 分鐘）

### 步驟 A：在手機訂閱推播

1. **手機開 Safari (或 Chrome)** → 開 https://www.vibelist.work
2. **登入你的帳號**（用你之前註冊的 email）
3. **進到 Settings** → 找「推播通知」或「Notifications」
4. **按「啟用推播」** → 瀏覽器會跳通知授權 → 點「**允許**」
5. **訂閱成功** → 你會看到「✅ 推播已啟用」狀態

> 這一步**無法被 AI 取代** — 通知權限必須由你手動授予瀏覽器。

### 步驟 B：驗證 DB 有訂閱紀錄

**手機** Chrome/Safari 開新分頁 → 貼這段 DevTools script（驗證 DB 寫入）：

```javascript
// 先登入後才能用 supabase client
// 在 Settings 頁開啟 DevTools (Safari: 進階 → 網頁開發工具 → 顯示)
const { data } = await window.supabase
  .from('push_subscriptions')
  .select('id, endpoint, is_active, created_at')
  .eq('is_active', true);
console.log('訂閱數:', data?.length);
console.log('首筆 endpoint:', data?.[0]?.endpoint);
```

如果看到 `訂閱數: 1` → DB 寫入成功。

### 步驟 C：觸發測試推播

打開手機的 Chrome / Safari → 在網址列輸入（觸發後端 API）：

```
javascript:fetch('https://www.vibelist.work/api/push/send',{method:'POST',headers:{'Content-Type':'application/json','x-internal-push-secret':'a87689f2b183cbd6922eaabdc45444230afa89f7423338ef584b8a8126a995e6'},body:JSON.stringify({owner_uid:'你的-user-uid',title:'TaskFlow 測試',body:'收到就成功',url:'/'})}).then(r=>r.json()).then(console.log)
```

> ⚠️ `你的-user-uid` 換成步驟 B 查到的 owner_uid（在 push_subscriptions 表的 owner_uid 欄位）

### 步驟 D：手機收到推播

幾秒內你手機應該跳出：
- 標題：**TaskFlow 測試**
- 內容：**收到就成功**
- 點擊 → 跳到 https://www.vibelist.work/

**看到這個推播 = 終點達成**。

### 步驟 E（可選）：驗證 cron 明天 9 點自動跑

明天 9:00 後 → 在 Supabase Dashboard → Table Editor → `notification_log` → 看有沒有自動產生的推播紀錄。
- 有 → 整套系統完成
- 沒有 → `cron_runs` 表看 cron job 有沒有跑（有可能是 Vercel Hobby plan 漏跑，需手動觸發）

---

## 🚨 如果步驟 A-F 失敗

| 症狀 | 原因 | 修法 |
|---|---|---|
| Settings 看不到「啟用推播」按鈕 | 該 user 沒訂閱 UI（被設計給 admin 用） | 直接進 `/settings/notifications` 試試 |
| 訂閱按鈕按了沒反應 | 通知權限已被禁用 | 瀏覽器設定 → 通知 → 找 vibelist.work → 改為「允許」 |
| 步驟 C curl 收到 `sent: 0` | `owner_uid` 填錯 / DB 沒該 user 的訂閱 | 重新對照步驟 B 的 `owner_uid` |
| 步驟 C curl 收到 401 / 403 | INTERNAL_PUSH_SECRET 不對 | 確認 header 名稱是 `x-internal-push-secret`（小寫） |
| 步驟 D 沒收到通知 | VAPID 簽章失敗 / push service 不接受 | 查 Supabase `notification_log` 看 status 是 `sent` 還是 `failed` |

---

## 📊 系統現狀

```
[前端]        vite bundle 已打包新公鑰 ✅
[後端]        Vercel production 跑新私鑰 ✅
[DB]          push_subscriptions 空（等你訂閱）
[cron]        明天 9:00 自動跑 fan_out_queue
[VAPID]       旋轉完畢，後端測試可成功送出
[middleware]  /api/push/* /api/cron/* 已加白名單
```

---

## 🪞 架構師鏡子

- **失序點 1**：`/api/push/send` 依賴「瀏覽器 + 通知權限」這層**無法被 server 自動化**的依賴，導致 E2E 驗證天生需要真實裝置。
- **失序點 2**：middleware 原本把所有非白名單都 redirect 到 /login，連「內部觸發 push API」也被擋 — 這個 bug **不修正就無法用 curl 測推播**。我已修。
- **失序點 3**：VAPID 公鑰原本**只有一份寫在前端 bundle**，旋轉時要 commit + deploy — 沒有 runtime 切換機制。建議未來改用 `NEXT_PUBLIC_VAPID_PUBLIC_KEY` 從 env 注入。

---

## 🚨 失敗待定位 — iOS Safari 通知授權視窗不彈（2026-08-02 新增）

| 項目 | 內容 |
|---|---|
| 症狀 | iOS Safari 一般 tab 按「開啟通知」不彈授權視窗；強制重置 SW + 清 Safari 網站資料後仍不彈 |
| 已試過 | (1) PWA 強制重置 SW + 清 push_subscriptions DB (commit `2842cf1`/`48fbb33`/`8775fe2`/`c0d9e30`/`45c5234`) (2) 路線A：Safari 一般 tab → 「aA → 網站設定 → 通知」 (3) iOS 設定 → Safari → 進階 → 網站資料 → 清除 vercel domain |
| 設備 | iPhone iOS 16.4+（支援 Web Push） |
| 下次試 | (1) Mac Chrome 端驗證後端推播邏輯（§27 重構方向先不嘗試，純環境確認）(2) iOS 設定 → 一般 → 重置 → 重置網站與隱私權資料（比 Safari 進階更深層）(3) 完全移除 PWA（主畫長按 → 刪除）+ Safari 設定 → 清除所有網站資料後重裝 |
| score | 6.5（環境問題，不是程式碼問題；後端已 OK） |
| §26 命中 | K（Safari 一般 tab vs PWA 行為不同）+ L（feature-detect / 平台支援）|

---

## 📋 待實作功能 — Day 1 Onboarding 新手引導

| 項目 | 內容 |
|---|---|
| 需求 | 完成 Onboarding（角色選擇 + 套用範本）後，新用戶仍有「Day 1 該做什麼」的空白——沒有下一步引導，不知道 PWA 可以安裝，不知道怎麼建立第一個任務 |
| 現有資源 | `Onboarding.tsx`（角色選擇 + 套用範本）✅、`PwaPrompts.tsx`（iOS/Android 安裝提示）✅、`AhaMoment`（完成第一個任務時慶祝）✅、`OnboardingTask.tsx`（空任務時注入 PWA 任務）⚠️（只在略過 Onboarding 時才觸發）|
| 缺口 | Onboarding 完成後 → 沒有「接下來做什麼」的引導；PWA 任務只在略過 Onboarding 時才注入，完成 Onboarding 的新用戶**完全收不到** |
| 狀態 | ✅ 已完成 `fd27a07` — Day1Guide 三步驟引導卡片 |

---

_Generated at: 2026-08-01 22:16 UTC+8_

---

# TaskFlow UI/UX 系統化（2026-08-11）

> **狀態：✅ 完成**
> 依據：AI review 反饋（按鈕系統、A11y、動畫管理）

## ✅ 已完成優化

| # | 項目 | 變更 |
|---|---|---|
| A1 | Accessibility 補強 | TaskCard tag chips 加 `aria-label="標籤: {tag}"` |
| B1 | 按鈕系統統一 | 新建 `src/components/ui/Button.tsx`，覆蓋全專案 ~16 處 `.btn-primary` / `.btn-ghost` 替換 |
| A2 | 微互動抽離確認 | 所有 `@keyframes` 已集中在 `globals.css` ANIMATIONS 區段，無 JSX inline |

**新增 Button API：**
```
variant="primary" | "ghost" | "danger"
size="sm" | "md" | "lg" | "icon-sm"
loading={bool}
icon={<Icon />} + iconPosition="left"|"right"
```

**特殊按鈕保留（職責分離）：**
- `ProGhostButton` → 功能鎖定（Pro gate）
- `GhostButton` → 假門測試（waitlist）
- `not-found.tsx` / `global-error.tsx` → 保留 `.btn-primary` 靜態 class（Error Boundary 特殊場景）

---

## 🪞 Architect Mirror

- [PASS] 無新失序揭露

---

# ICS 假日來源 — SSOT 替換長期計畫（2026-09-14）

> **狀態：⏳ 待規劃（C-3 短期已上線,A 方案尚未啟動）**

## 背景

當前 `deriveHolidayPrefix` 完全信任 Google「台灣的節慶假日」ICS 的 DESCRIPTION 欄位區分全民放假 vs 紀念日。但 DGPA 在 **114 年 6 月 13 日公告**修法,新增 3 個國定假日:

| 日期 | 假日名稱 | Google ICS 標記 | 實際應為 |
|---|---|---|---|
| 9/28 | 教師節 / 孔子誕辰紀念日 | 假日節慶 ❌ | ★ 全民放假 |
| 10/25 | 台灣光復節 / 臺灣光復暨金門古寧頭大捷紀念日 | 假日節慶 ❌ | ★ 全民放假 |
| 12/25 | 行憲紀念日 | 假日節慶 ❌ | ★ 全民放假 |

**根因**:Google ICS 沒跟上 2025 年的「紀念日及節日實施條例」修法。短期用 §F4 override 白名單(C-1 已完成)止血;長期要把 SSOT 換到 DGPA。

## C-3 短期(已完成 ✅)

- [x] `deriveHolidayPrefix` 加 `SUMMARY_FULL_HOLIDAY_OVERRIDE` 白名單
- [x] 加 4 個測試(教師節 / 光復節 / 行憲 / 軍人節不被誤傷)
- [x] 測試 13/13 通過,tsc 乾淨(僅 logger.test.ts 4 個 pre-existing error 無關)

## A 長期(待規劃 ⏳)

DGPA 只發 PDF / Excel,不發 ICS 訂閱 URL。可行方案:

| 方案 | 描述 | 成本 | 風險 |
|---|---|---|---|
| A-1 | Build-time 腳本:每年 6 月抓 DGPA PDF → 解析 → 生成 static ICS 放 `/public/tw-holidays.ics` | 中（PDF 解析要 OCR 或表格抽取） | DGPA PDF 格式變更要重寫解析 |
| A-2 | 後端 endpoint `/api/tw-holidays`:存一份結構化 JSON 在 repo,執行時組裝成 ICS | 低 | 每年要手動 commit 新版本 |
| A-3 | 訂閱其他第三方 ICS(民間專案如 taiwan-calendar.ics) | 低 | 依賴外部專案維護 |
| A-4 | 完全內建 JS constant map,放棄 ICS 訂閱依賴 | 低 | 跨年失效,每年要更新 |

**建議優先**:A-1(自動化最完整)→ 失敗 fallback A-2(最簡單可控)。

## 🪞 Architect Mirror

- [SMELL] `優化清單.md` 第 211 行結尾有個孤兒底線 `揭露_`(history artifact)
- [SMELL] `優化清單.md` 結構混亂(任務紀錄 vs 規劃混在一起),未來應考慮拆 `history/` 子目錄
- [PASS] C-1 override 是「資料層修補」而非「邏輯層改寫」,符合修補最小化原則

