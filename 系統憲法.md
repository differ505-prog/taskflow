# 系統憲法（濃縮版）

> 設計基調：功能極簡 × 視覺優雅 × 工程嚴謹
> 定位：TickTick 同類任務管理網站

---

## 一、核心死罪（約束帶來真實傷害的規則）

### §5 失物招領防線（情緒保護）

觸發 `LostAndFound.tsx` 新增或修改時：

- **禁止任何 red 色系**（`bg-red-*` / `text-red-*` 等）→ ADHD 用戶焦慮迴路
- **禁止驚嘆號、抖動動畫、倒計時** → 恐慌 = 逃避
- **禁止 Cron / setInterval 自動刪除或標記過期** → 用戶必須親手按下按鈕才叫放下
- **禁止責備語氣**（「過期」「未完成」「逾期」）→ 情緒保護殼
- `LostAndFound` 組件**只能在 `currentView === "inbox"` 下顯示**

### §5a 流程鎖定（工作流保護）

單一任務動作**禁止**同時觸發：
- §F1：`router.push` / `setCurrentView` 改變視圖（多選時需保留原視圖）
- §F2：搶 Zen 焦點（`order = -1`）
- §F3：modal / bottom sheet 阻擋後續操作

例外：使用者明確點擊全域 CTA（「清空失物招領」「進入 Zen」）。

### §11 push 安全

- `git push --force` / `--force-with-lease` → **口頭確認**
- `git reset --hard` / `git clean -fd` / 刪除檔案 / 覆寫 `.env` → **口頭確認**
- Commit message **必填** `deploy:` 前綴：
  - `deploy: main` → 觸發 production deployment
  - `deploy: preview-only` → 只觸發 preview
  - `deploy: skip-deploy` → 純 docs / config

### §12 驗證必讀結果

`tsc --noEmit` / `npm run build` 後，**必須讀完整 stderr / exit code**：
- ✅ `tsc: clean / build: success`
- ❌ `tsc: N errors / build: failed`

禁止「跑完就當成功」。

### §23 同步層確認

涉及「資料同步」「雲端寫入」「realtime」時，**先確認哪個模組真正在處理**（FirebaseDataProvider / SyncWriter / personalTaskSync / Supabase Realtime），再動手。

---

## 二、觸發式鉤子表（符合條件才執行，不要沒事套用）

### Debug 失敗循環攔截

| 觸發症狀 | 執行什麼 | 原因 |
|---|---|---|
| 用戶說「還沒修好」| §16b + §18d 重述，不立刻動手 | 避免悶頭燒 token |
| 同一路徑失敗 2 次 | §27 評分表必含「重構大改」選項 | 避免同路徑再試 |
| 連續 3 輪無實質新進展 | §16b.1 停下，報告已試 / 卡在哪 / 建議 | 避免兔子洞 |
| Debug 前 | 先讀 `優化清單.md`，從 SSOT 接續 | 避免從零推理 |

### UI Bug 修正

| 觸發症狀 | 執行什麼 | 原因 |
|---|---|---|
| 用戶附截圖描述症狀 | 先對齊截圖觀察，根因表 ≤3 行 | §29 避免過度推理 |
| 用戶說「看清楚截圖」| 截圖觀察優先，不從零推理祖層 | 避免浪費 10+ tool call |
| JSX 條件渲染改動 | grep 確認外層 ternary 切換點 | §14.2 避免修內層無效 |
| 多處 JSX 變動 >3 處 | 先畫 DOM 巢狀地圖再動手 | §14.3 避免 `</div>` 擺錯 |

### Runtime 驗證

| 觸發條件 | 上限 | 說明 |
|---|---|---|
| 純 Tailwind / CSS 改動 | **0 tool call** | 用戶截圖確認即可 |
| Component-local state / handler | **2 tool call** | console.log + 用戶確認 |
| 跨元件 / Zustand / API | **3 tool call** | CDP + 完整流程測試 |
| 累計 2 次工具失敗 | **停下報告** | §15.5 失敗上限 |

> CDP / 瀏覽器互動**預設禁用**。啟用條件：① 有明確假設 ② 靜態分析仍無法定位 ③ 用戶**明確要求**（模糊詞不算）。

### 變數名確認

| 觸發條件 | 執行什麼 |
|---|---|
| 寫 DevTools script 給用戶跑 | 先 grep 確認所有變數名（storage key / DOM selector / state 名），禁止猜 |

### 已知根因類別速查

| 類別 | 典型症狀 | 標準修法 |
|---|---|---|
| **A** | 第一次 tap 跳回原狀 | 5 秒寫入保護窗 |
| **B** | flex child 高度塌縮 | 祖先鏈移除 `overflow: hidden` |
| **C** | IME Enter 沒清空、新增失敗 | 改 `onKeyUp` 或 `onChange` |
| **E** | 修了 sync 沒生效 | 先確認 sync 層是誰（§23） |
| **F** | 新增後重整消失 | skipFirst callback |
| **N** | 整排 UI 一起消失 | 拆外層 ternary，永遠渲染 toolbar |
| **O** | ESC handler 後面板再也打不開 | 雙 hook 獨立 state 死鎖，需 reset |
| **Q** | 中文 NLP 日期解析失效 | 中文字 pattern 禁 `\b`，直接寫 `今天`、`明天` |
| **R** | 內層 scroll 卡死 / rubber band | 用 `scrollTop === 0` 而非 `window.scrollY` |
| **V** | reorder / drag 按了沒反應 | selector 要 sort by `order` 欄位 |

---

## 三、已廢除的冗長描述（不再執行）

以下條文的**精神已濃縮進鉤子表**，具體條文廢除：

- ~~§10 評分表（完整 4 候選格式）~~ → 鉤子表 Debug 失敗循環
- ~~§14.1–14.4 修法步驟~~ → 濃縮進 Debug 鉤子表
- ~~§17.1 Architect Mirror~~ → commit 前揭露改為可選
- ~~§18 / §18b / §18c / §18d / §18d.1~~ → 濃縮進觸發式鉤子
- ~~§26 完整 15+ 類別 + 例子~~ → 速查表，減少搜尋成本
- ~~§27 / §27.1 工作流公約~~ → 濃縮進 Debug 鉤子表
- ~~§28 Runtime 工具預設禁用~~ → 濃縮進 Runtime 驗證表
- ~~§30 後續輪次對齊~~ → 鉤子表「還沒修好」行
- ~~§31 圖片辨識強制~~ → 已內建，無需重述

---

## 四、ADHD Mode 整合

當用戶已啟動 `/i-have-adhd` 時：

1. **§18d 重述可跳過**（用戶已給明確任務時）
2. **§10 評分表降到 1 個首選**（不做完整 4 候選）
3. **分析鍊只走一次**：靜態分析失敗 → 直接問 runtime 證據，不做第二輪猜測
4. **每個回覆結束時**：給出下一步 ONE action，≤2 分鐘可完成

---

## 五、Commit Message 格式

```
<類型>(<範圍>): 摘要 [deploy: <main|preview-only|skip-deploy>]

🪞 Architect Mirror (this commit):
- [PASS] 無失序需要揭露
- [SMELL] 列出失序：<檔案:行號 + 簡述>
```

> Architect Mirror 改為**可選揭露**，不強迫每次 commit 都填。

---

## 六、部署目標 Gate

| 改動類型 | deploy 值 |
|---|---|
| UI fix / 功能新增 | `deploy: main` |
| 實驗性 / preview branch | `deploy: preview-only` |
| 純 docs / config | `deploy: skip-deploy` |
| `git push --force` 主分支 | **口頭確認** |

---

## 七、待實作功能（SSOT）

檔案：`優化清單.md`

## 八、系統憲法變更紀錄

| 日期 | 變更 | 自評分 |
|---|---|-----|
| 2026-09-14 | 濃縮版：核心死罪 + 觸發式鉤子表 + 廢除冗長描述 + ADHD 整合 | — |

